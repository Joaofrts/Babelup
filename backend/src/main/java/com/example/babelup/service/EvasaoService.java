package com.example.babelup.service;

import org.springframework.stereotype.Service;

@Service
public class EvasaoService {

   /* public boolean verificarRiscoEvasao(Usuario aluno) {


        boolean baixaFrequencia = calcularFrequencia(aluno.getId());

        boolean estagnado = calcularEstagnacao(aluno.getId());

        boolean notasBaixas = calcularMediaNotas(aluno.getId());

        return baixaFrequencia && estagnado && notasBaixas;
    }


    private boolean calcularFrequencia(Long alunoId) {
        return true;
    }
    private boolean calcularEstagnacao(Long alunoId) {
        return true;
    }
    private boolean calcularMediaNotas(Long alunoId) {
        return true;
    }*/
}
